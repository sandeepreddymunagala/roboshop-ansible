- name: Install MySQL Client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: Load Schema
  ansible.builtin.shell: mysql -h mysql-dev.trydevops13.shop -uroot -pRoboShop@1 </app/schema/{{ component }}.sql



